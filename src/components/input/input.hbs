<div class="form__group">
	<input type="{{type}}" name="{{name}}" id="{{name}}" class="form__input {{class1}}" placeholder="{{placeholder}}">
	<label for="{{name}}" class="form__label">{{label}}</label>
</div>